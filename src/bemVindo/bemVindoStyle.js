import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
    bemVindoText: {
        fontSize: 30,
        fontWeight: 'bold',
        marginLeft: 40,
        marginTop: 10,
    },
    bemVindoView: {

    }
});

export default styles;
