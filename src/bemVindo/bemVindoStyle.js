import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
    bemVindoText: {
        fontSize: 30,
        fontWeight: 'bold',
        marginLeft: 40,
        marginTop: 10,
        color: '#fff'
    },
    bemVindoView: {

    }
});

export default styles;
